pipeline {
    agent any
    
    environment {
        NODE_VERSION = '18.20.1'
        PATH = "/tmp/node-v18.20.1-linux-x64/bin:${env.PATH}"
    }
    
    options {
        timeout(time: 30, unit: 'MINUTES')
        timestamps()
    }
    
    stages {
        stage('Checkout') {
            steps {
                echo "🔄 Usando código fuente del workspace..."
                sh '''
                    echo "📋 Información del Build:"
                    echo "Build: ${BUILD_NUMBER}"
                    echo "Workspace: $(pwd)"
                    echo "Usuario: $(whoami)"
                    echo "Fecha: $(date)"
                    echo "📂 Contenido del workspace:"
                    ls -la
                '''
            }
        }
        
        stage('Check System Dependencies') {
            steps {
                echo "🔍 Verificando herramientas del sistema..."
                sh '''
                    echo "📊 Información del sistema:"
                    echo "Sistema: $(uname -a)"
                    echo "Usuario: $(whoami)"
                    echo "Directorio: $(pwd)"
                    echo "Distribución: $(cat /etc/os-release | head -2 2>/dev/null || echo 'No disponible')"
                    
                    # Verificar herramientas esenciales (ya disponibles en la mayoría de sistemas)
                    echo "🔍 Verificando herramientas básicas:"
                    which curl && echo "✅ curl disponible" || echo "❌ curl no disponible"
                    which wget && echo "✅ wget disponible" || echo "❌ wget no disponible"
                    which tar && echo "✅ tar disponible" || echo "❌ tar no disponible"
                    which gzip && echo "✅ gzip disponible" || echo "❌ gzip no disponible"
                    
                    # Verificar si tenemos al menos una herramienta de descarga
                    if ! command -v curl >/dev/null 2>&1 && ! command -v wget >/dev/null 2>&1; then
                        echo "❌ ERROR: No hay curl ni wget disponible"
                        echo "💡 Solución: Instalar curl o wget en el sistema Jenkins"
                        exit 1
                    fi
                    
                    echo "✅ Herramientas básicas verificadas"
                '''
            }
        }
        
        stage('Install Node.js') {
            steps {
                echo "📥 Instalando Node.js..."
                sh '''
                    # Verificar si Node.js ya está disponible
                    if command -v node >/dev/null 2>&1; then
                        echo "✅ Node.js ya disponible: $(node --version)"
                    else
                        echo "📥 Node.js no encontrado, instalando en /tmp..."
                        
                        # Crear directorio temporal
                        mkdir -p /tmp/node-install
                        cd /tmp/node-install
                        
                        # Detectar arquitectura
                        ARCH=$(uname -m)
                        case $ARCH in
                            x86_64)
                                NODE_ARCH="x64"
                                ;;
                            aarch64)
                                NODE_ARCH="arm64"
                                ;;
                            *)
                                echo "❌ Arquitectura no soportada: $ARCH"
                                exit 1
                                ;;
                        esac
                        
                        echo "🏗️ Arquitectura: $ARCH -> Node.js $NODE_ARCH"
                        
                        # Usar tar.gz para máxima compatibilidad
                        NODE_FILE="node-v${NODE_VERSION}-linux-${NODE_ARCH}.tar.gz"
                        NODE_URL="https://nodejs.org/dist/v${NODE_VERSION}/${NODE_FILE}"
                        
                        echo "📥 Descargando desde: $NODE_URL"
                        
                        # Intentar descargar con curl o wget
                        if command -v curl >/dev/null 2>&1; then
                            curl -fsSL "$NODE_URL" -o "$NODE_FILE"
                        elif command -v wget >/dev/null 2>&1; then
                            wget -q "$NODE_URL" -O "$NODE_FILE"
                        else
                            echo "❌ No hay curl ni wget disponible"
                            exit 1
                        fi
                        
                        # Verificar descarga
                        if [ ! -f "$NODE_FILE" ]; then
                            echo "❌ Error al descargar Node.js"
                            exit 1
                        fi
                        
                        echo "📦 Extrayendo Node.js..."
                        tar -xzf "$NODE_FILE"
                        
                        # Verificar extracción
                        NODE_DIR="node-v${NODE_VERSION}-linux-${NODE_ARCH}"
                        if [ ! -d "$NODE_DIR" ]; then
                            echo "❌ Error al extraer Node.js"
                            exit 1
                        fi
                        
                        # Mover a ubicación estándar temporal
                        mv "$NODE_DIR" "/tmp/node-v${NODE_VERSION}-linux-${NODE_ARCH}"
                        
                        echo "✅ Node.js instalado en /tmp/node-v${NODE_VERSION}-linux-${NODE_ARCH}"
                    fi
                    
                    # Verificar instalación
                    export PATH="/tmp/node-v${NODE_VERSION}-linux-${NODE_ARCH}/bin:$PATH"
                    echo "🔍 Verificando instalación:"
                    node --version
                    npm --version
                '''
            }
        }
        
        stage('Project Structure') {
            steps {
                echo "📂 Analizando estructura del proyecto..."
                sh '''
                    # Asegurar que Node.js esté en el PATH
                    export PATH="/tmp/node-v${NODE_VERSION}-linux-${NODE_ARCH}/bin:$PATH"
                    
                    echo "🔍 Verificando estructura del proyecto:"
                    
                    if [ -d "test_app" ]; then
                        echo "✅ Directorio test_app encontrado"
                        cd test_app
                        
                        echo "📂 Contenido de test_app:"
                        ls -la
                        
                        # Verificar archivos principales
                        echo "📊 Archivos del proyecto:"
                        [ -f "package.json" ] && echo "  ✅ package.json" || echo "  ❌ package.json"
                        [ -f "index.js" ] && echo "  ✅ index.js" || echo "  ❌ index.js"
                        [ -f "index.test.js" ] && echo "  ✅ index.test.js" || echo "  ❌ index.test.js"
                        [ -f "pubspec.yaml" ] && echo "  ✅ pubspec.yaml (Flutter)" || echo "  ⚠️ pubspec.yaml no encontrado"
                        [ -d "lib" ] && echo "  ✅ lib/ (Flutter)" || echo "  ⚠️ lib/ no encontrado"
                        
                        # Mostrar package.json si existe
                        if [ -f "package.json" ]; then
                            echo "📄 Contenido de package.json:"
                            cat package.json
                            
                            # Validar JSON básico
                            echo "🔍 Validando package.json..."
                            if node -e "JSON.parse(require('fs').readFileSync('package.json', 'utf8'))"; then
                                echo "  ✅ package.json es válido"
                            else
                                echo "  ❌ package.json tiene errores"
                                exit 1
                            fi
                        fi
                        
                    else
                        echo "❌ Directorio test_app no encontrado"
                        echo "📂 Contenido actual:"
                        ls -la
                        exit 1
                    fi
                '''
            }
        }
        
        stage('Install Dependencies') {
            steps {
                echo "📦 Instalando dependencias del proyecto..."
                dir('test_app') {
                    sh '''
                        # Asegurar que Node.js esté en el PATH
                        export PATH="/tmp/node-v${NODE_VERSION}-linux-${NODE_ARCH}/bin:$PATH"
                        
                        if [ -f "package.json" ]; then
                            echo "📥 Instalando dependencias de Node.js..."
                            
                            # Mostrar información previa
                            echo "📊 Información previa:"
                            echo "Node.js: $(node --version)"
                            echo "npm: $(npm --version)"
                            echo "Directorio: $(pwd)"
                            
                            # Instalar dependencias
                            npm install
                            
                            # Verificar instalación
                            echo "📦 Verificando instalación:"
                            [ -d "node_modules" ] && echo "✅ node_modules creado" || echo "❌ node_modules no creado"
                            
                            # Listar dependencias instaladas
                            echo "📊 Dependencias instaladas:"
                            npm list --depth=0 || echo "⚠️ Algunas dependencias pueden tener problemas menores"
                            
                        else
                            echo "⚠️ No hay package.json, saltando instalación de dependencias"
                        fi
                    '''
                }
            }
        }
        
        stage('Verify Installation') {
            steps {
                echo "🔍 Verificando instalación completa..."
                dir('test_app') {
                    sh '''
                        # Asegurar que Node.js esté en el PATH
                        export PATH="/tmp/node-v${NODE_VERSION}-linux-${NODE_ARCH}/bin:$PATH"
                        
                        echo "📊 Estado del entorno:"
                        echo "Node.js: $(node --version)"
                        echo "npm: $(npm --version)"
                        echo "Directorio: $(pwd)"
                        
                        echo "📂 Archivos del proyecto:"
                        ls -la
                        
                        echo "📦 Estado de node_modules:"
                        if [ -d "node_modules" ]; then
                            echo "✅ node_modules existe"
                            echo "📊 Tamaño: $(du -sh node_modules)"
                            echo "📈 Cantidad de módulos: $(ls node_modules | wc -l)"
                        else
                            echo "❌ node_modules no existe"
                        fi
                        
                        # Verificar archivos principales
                        echo "🔍 Verificando archivos principales:"
                        for file in index.js index.test.js; do
                            if [ -f "$file" ]; then
                                echo "📄 $file:"
                                echo "  ✅ Existe"
                                echo "  📊 Tamaño: $(wc -l < "$file") líneas"
                                
                                # Verificar sintaxis básica
                                if node -c "$file" 2>/dev/null; then
                                    echo "  ✅ Sintaxis válida"
                                else
                                    echo "  ❌ Error de sintaxis"
                                fi
                            else
                                echo "📄 $file: ❌ No encontrado"
                            fi
                        done
                    '''
                }
            }
        }
        
        stage('Run Tests') {
            steps {
                echo "🧪 Ejecutando tests..."
                dir('test_app') {
                    sh '''
                        # Asegurar que Node.js esté en el PATH
                        export PATH="/tmp/node-v${NODE_VERSION}-linux-${NODE_ARCH}/bin:$PATH"
                        
                        if [ -f "index.test.js" ]; then
                            echo "✅ Archivo de tests encontrado"
                            
                            # Verificar si hay script de test en package.json
                            if [ -f "package.json" ] && grep -q '"test"' package.json; then
                                echo "🚀 Ejecutando tests con npm test..."
                                npm test
                            else
                                echo "🚀 Ejecutando tests directamente..."
                                node index.test.js
                            fi
                        else
                            echo "⚠️ No se encontró archivo de tests"
                            echo "📂 Archivos disponibles:"
                            ls -la *.js 2>/dev/null || echo "No hay archivos .js"
                        fi
                    '''
                }
            }
        }
        
        stage('Code Analysis') {
            steps {
                echo "🔍 Análisis básico del código..."
                dir('test_app') {
                    sh '''
                        # Asegurar que Node.js esté en el PATH
                        export PATH="/tmp/node-v${NODE_VERSION}-linux-${NODE_ARCH}/bin:$PATH"
                        
                        echo "📊 Análisis de archivos JavaScript:"
                        for file in *.js; do
                            if [ -f "$file" ]; then
                                echo "🔍 Analizando $file:"
                                echo "  📊 Líneas: $(wc -l < "$file")"
                                echo "  📊 Caracteres: $(wc -c < "$file")"
                                
                                # Verificar sintaxis
                                if node -c "$file"; then
                                    echo "  ✅ Sintaxis correcta"
                                else
                                    echo "  ❌ Error de sintaxis"
                                fi
                            fi
                        done
                        
                        # Información del proyecto
                        if [ -f "package.json" ]; then
                            echo "📦 Información del proyecto:"
                            node -e "
                                const pkg = require('./package.json');
                                console.log('  Nombre:', pkg.name || 'No especificado');
                                console.log('  Versión:', pkg.version || 'No especificado');
                                console.log('  Descripción:', pkg.description || 'No especificado');
                                console.log('  Dependencias:', Object.keys(pkg.dependencies || {}).length);
                                console.log('  DevDependencies:', Object.keys(pkg.devDependencies || {}).length);
                            "
                        fi
                    '''
                }
            }
        }
        
        stage('Build Summary') {
            steps {
                echo "📋 Resumen del build..."
                sh '''
                    # Asegurar que Node.js esté en el PATH
                    export PATH="/tmp/node-v${NODE_VERSION}-linux-${NODE_ARCH}/bin:$PATH"
                    
                    echo "✅ Build completado exitosamente"
                    echo ""
                    echo "📊 Resumen:"
                    echo "  🔧 Node.js: $(node --version)"
                    echo "  📦 npm: $(npm --version)"
                    echo "  📂 Directorio: $(pwd)"
                    echo "  🕐 Fecha: $(date)"
                    echo ""
                    echo "📈 Estadísticas del proyecto:"
                    if [ -d "test_app" ]; then
                        cd test_app
                        echo "  📄 Archivos JS: $(find . -name "*.js" -type f | wc -l)"
                        echo "  📦 node_modules: $([ -d "node_modules" ] && echo "✅ Instalado" || echo "❌ No instalado")"
                        echo "  🧪 Tests: $([ -f "index.test.js" ] && echo "✅ Disponibles" || echo "❌ No encontrados")"
                    fi
                '''
            }
        }
    }
    
    post {
        always {
            echo "🧹 Limpiando recursos temporales..."
            sh '''
                echo "🗑️ Limpiando instalación temporal de Node.js..."
                rm -rf /tmp/node-install
                rm -rf /tmp/node-v${NODE_VERSION}-linux-*
                echo "✅ Limpieza completada"
            '''
        }
        
        success {
            echo """
                ✅ Pipeline completado exitosamente!
                
                🎉 Resumen del éxito:
                - Proyecto: proyecto_clima_Pipeline
                - Build: #${BUILD_NUMBER}
                - Estado: SUCCESS ✅
                
                ✅ Etapas completadas:
                1. ✅ Checkout del código
                2. ✅ Instalación de herramientas del sistema
                3. ✅ Instalación de Node.js
                4. ✅ Análisis de estructura del proyecto
                5. ✅ Instalación de dependencias
                6. ✅ Verificación de instalación
                7. ✅ Ejecución de tests
                8. ✅ Análisis de código
                9. ✅ Resumen del build
            """
        }
        
        failure {
            echo """
                ❌ Pipeline falló!
                
                💥 Información del error:
                - Proyecto: proyecto_clima_Pipeline
                - Build: #${BUILD_NUMBER}
                - Estado: FAILURE ❌
                
                🔧 Pasos para diagnóstico:
                1. Revisar los logs de la etapa que falló
                2. Verificar que Jenkins tenga permisos de administrador
                3. Comprobar conectividad a internet
                4. Verificar estructura del proyecto
                
                💡 Verificaciones comunes:
                - ¿Jenkins puede ejecutar como root?
                - ¿Hay acceso a internet para descargar Node.js?
                - ¿El directorio test_app existe en el repositorio?
                - ¿El archivo package.json es válido?
            """
        }
    }
}