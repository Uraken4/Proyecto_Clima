pipeline {
  agent any
  stages {
    stage('Install') {
      steps {
        sh 'npm install'
      }
    }
    stage('Test') {
      steps {
        sh 'npm test'
      }
    }
    stage('Build Docker') {
      steps {
        sh 'docker build -t proyecto_clima .'
      }
    }
    // Puedes agregar más stages para push o deploy si lo necesitas
  }
}
